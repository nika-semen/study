#include "stdio.h"

void main()
{
	//задача є7
	/*ћножество Ц это количество предметов или чисел, обладающих общими свойствами.
	ћножества не содержат повтор€ющихс€ элементов.*/
	int a1, a2, a3, b, i, sum = 0;
	scanf_s("%d%d%d", &a1, &a2, &a3);
	for (i = 0; i < 3; i++)
	{
		scanf_s("%d", &b);
		if (b == a1) { sum++; }
		if (b == a2) { sum++; }
		if (b == a3) { sum++; }
	}
	printf("%d", sum);
}